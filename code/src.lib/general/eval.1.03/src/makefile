# Makefile for eval library
# =========================
# by R.J.Barnes
#
#
INCLUDE=-I$(IPATH)/general
SRC=eval.c
OBJS = eval.o
DSTPATH=$(LIBPATH)
OUTPUT = reval
LINK="1"
include $(MAKELIB)

















